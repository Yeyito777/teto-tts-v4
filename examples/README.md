# examples

Generated audio examples are intentionally not tracked in git.

The accepted examples and their local paths are documented in:

```text
../DATASET_STYLE.md
../CURRENT_STACK.md
```

In the working local copy, this directory contains symlinks to generated Fish S2-Pro examples. In the public repo, audio files are ignored because they may be copyrighted, large, or environment-specific.
